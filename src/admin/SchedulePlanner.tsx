import React from "react";
import AdminModuleShell from "./Components/AdminModuleShell";

export default function SchedulePlanner() {
  return (
    <AdminModuleShell
      title="Schedule Planner"
      description="Schedule planning entrypoint prepared in the admin area so calendar, staffing, and operational planning can be expanded cleanly."
    />
  );
}
