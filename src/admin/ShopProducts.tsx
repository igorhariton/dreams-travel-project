import React from "react";
import AdminModuleShell from "./Components/AdminModuleShell";

export default function ShopProducts() {
  return (
    <AdminModuleShell
      title="Shop Products"
      description="Product management entrypoint prepared in src/admin for future catalog, inventory, and merchandising features."
    />
  );
}
