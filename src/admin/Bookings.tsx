import React from "react";
import AdminModuleShell from "./Components/AdminModuleShell";

export default function Bookings() {
  return (
    <AdminModuleShell
      title="Bookings"
      description="Admin bookings module entrypoint prepared inside src/admin. This file keeps the admin area separated and ready for dedicated booking management screens."
    />
  );
}
