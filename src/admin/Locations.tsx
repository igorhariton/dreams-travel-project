import React from "react";
import AdminModuleShell from "./Components/AdminModuleShell";

export default function Locations() {
  return (
    <AdminModuleShell
      title="Locations"
      description="Locations admin entrypoint prepared for destination and operational location management within the dedicated admin zone."
    />
  );
}
