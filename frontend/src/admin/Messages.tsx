import React from "react";
import AdminModuleShell from "./Components/AdminModuleShell";

export default function Messages() {
  return (
    <AdminModuleShell
      title="Messages"
      description="Admin messaging module entrypoint prepared inside src/admin for moderation, support, and internal communication views."
    />
  );
}
