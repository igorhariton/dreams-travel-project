# Base URL for all frontend API calls
VITE_API_BASE_URL=/api

# App environment: development | staging | production | test
VITE_APP_ENV=development

# Enable local chat fallback only for local development debugging
# Effective only when VITE_APP_ENV=development
VITE_ENABLE_CHAT_FALLBACK=false
